# Agent

`agent` is a Go CLI for creating Codex-backed git worktrees and tmux sessions from a task prompt.

## V1 Scope

The first version targets:

- `agent new`
- `agent ls`
- `agent open`
- `agent status`
- `agent doctor`

Later phases will add task cleanup, richer provider support, and more orchestration features.
